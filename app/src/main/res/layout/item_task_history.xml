<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    card_view:cardCornerRadius="12dp"
    card_view:cardElevation="4dp"
    android:layout_marginHorizontal="8dp"
    android:backgroundTint="@android:color/white">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Task Name -->
        <TextView
            android:id="@+id/history_task_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Task Name"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="@android:color/black" />

        <!-- Pomodoro Count -->
        <TextView
            android:id="@+id/history_pomodoro_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/history_task_name"
            android:layout_marginTop="6dp"
            android:text="Pomodoros Used: 0"
            android:textSize="15sp"
            android:textColor="#444444" />

        <!-- Distraction Count -->
        <TextView
            android:id="@+id/history_distraction_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/history_pomodoro_count"
            android:layout_marginTop="4dp"
            android:text="Distractions: 0"
            android:textSize="15sp"
            android:textColor="#444444" />

        <!-- Delete Button -->
        <ImageButton
            android:id="@+id/delete_history_task_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignParentEnd="true"
            android:background="@android:color/transparent"
            android:layout_marginTop="4dp"
            android:src="@android:drawable/ic_menu_delete"
            android:contentDescription="Delete Task" />
    </RelativeLayout>
</androidx.cardview.widget.CardView>
